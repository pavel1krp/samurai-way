import React from "react";
import {LoginForm} from "./LoginForm";

type LoginPropsType ={

}

export const Login =(props:LoginPropsType)=>{
    return <div>
        <h1>Login</h1>
        <LoginForm/>
    </div>

}

